#!/bin/sh
# Select checks from Git's pre-push ref updates. Full CI remains the merge gate.
set -eu
repo_root=$(git rev-parse --show-toplevel)
cd "$repo_root"
exec python3 scripts/pre_push_checks.py --hook "$@"
