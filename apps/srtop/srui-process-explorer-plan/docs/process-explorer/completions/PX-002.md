# PX-002 implementation evidence

- Base: `d203f6187fc5da5fbf9e2da87b2a5f5a956b49a8` (`origin/main`).
- Branch: `codex/px-002-fake-20260912T1250`.
- Worktree: `/tmp/process-explorer-worktrees/20260912T1250/implementation`.
- Implementation acceptance: PASS, 12 September 2026, native macOS.
- Independent verification and final ticket commit: pending the orchestrator's fresh candidate verification. Changes are intentionally uncommitted under the assigned run's commit-timing override.
- External run evidence: `/tmp/process-explorer-run/20260912T1250/PX-002/implementation/`.

## Bounded behavior

`--fake-source` injects `FakeProcessSource` at startup. It produces exactly three fixed records in source order: `4101 / worker`, `4102 / worker`, `Unavailable / helper`. The existing default empty shell and `--smoke-fixture` title trigger remain covered.

`source.rs` defines domain records independently of SRUI values, with typed source identity, source-owned record tokens, UTC snapshot time and explicit known/missing values. The fake adapter constructs constants, including its timestamp; it imports no process collector and reads neither live process state nor the clock. The app invokes only its injected source once. The source record token is deliberately not an OS process-instance identity claim; PX-003 owns that audit.

`projection.rs` maps source/record identities to opaque session-local ItemIds independently of PID, display name and row position. Allocation never removes/recycles IDs; tests cover reordered/renamed records, changed PIDs, another source and another instance token. Duplicate source record identity is rejected before UI publication. Unavailable and denied values become explicit labels, while a known zero stays numeric zero.

The five semantic nodes remain unchanged in count. Records use ordinary `CREATE_MODEL`/`MODEL_INSERT` in the same atomic initialization transaction as the shell. No row child nodes, process actions, collection loop, new protocol, transport or production client logic were introduced.

`Cargo.toml` and `Cargo.lock` add only a direct local `srui-semantic-tree` dependency for the existing `ModelItem` type. All previously pinned versions/checksums remain unchanged.

References read: D1 §§6–8, 12, 22, 29; D2 T9–T11, T16, T21; BASELINE, standing contract, execution timeline, ticket/index and PX-000/PX-001 completion records. Existing SDK/model/renderer APIs were checked at the base. No new OS process API or upstream collector dependency is used.

## Verification

All commands run from the assigned worktree root; log names below resolve under the external run directory.

- `cargo test --locked --manifest-path apps/srtop/Cargo.toml` → exit 0; 12 tests, including domain determinism, one injected sample, exact ordered model values, distinct IDs, explicit missing/denied versus known zero, invalid identity rollback, existing shell and signal cleanup. `rust-app.log`.
- `PX002_EVIDENCE_DIR=/tmp/process-explorer-run/20260912T1250/PX-002/implementation/native-evidence bash apps/srtop/test.sh` → final exit 0; 12 Rust tests, app/SSH bridge builds and both empty/fake native SSH cases. `native-final.log`.
- `cargo clippy --locked --manifest-path apps/srtop/Cargo.toml --all-targets -- -D warnings` → exit 0. `clippy.log`.
- `cargo test --locked --manifest-path examples/process-monitor/Cargo.toml` → exit 0; 65 baseline example tests. `baseline.log`.
- `python3 scripts/run-conformance --suite 1 --implementation rust` → exit 0, PASS. `conformance-core-rust.log`.
- `python3 scripts/run-conformance --suite 1 --implementation swift` → exit 0, PASS. `conformance-core-swift.log`.
- `python3 scripts/run-conformance --suite 2` → exit 0; all four runners pass; existing documented Tree EXPANSION_CHANGED / Surface VIEWPORT_CHANGED gap remains outside this ticket. `conformance-widgets.log`.
- `python3 scripts/run-conformance --suite 3` → exit 0, both runners PASS. `conformance-semantic.log`.
- `python3 apps/srtop/srui-process-explorer-plan/validate_plan.py` → exit 0; 146 tickets / 48 seed families. `plan.log`.
- `python3 apps/srtop/srui-process-explorer-plan/validate_feature_ledger.py` → exit 0; baseline ledger validation. `baseline-ledger.log`.
- Direct `validate_ledger` checks for `feature-ledger.px002.json` → exit 0; valid ledger accepted and duplicate ID, missing reference, verified-without-evidence mutations rejected. `px002-ledger.log`.
- `bash scripts/check-test-process-stdio.sh` → exit 0. `stdio.log`.
- `cargo fmt --manifest-path apps/srtop/Cargo.toml -p srui-process-explorer --check` and `git diff --check` → exit 0.

## Native and mutation evidence

The unmodified generic production client receives the Rust app over a real authenticated localhost OpenSSH subsystem, pinned host key, existing bridge and non-PTY transport. Final fake run: visible NSWindow `482161`, revisions `1 → 2`, retained Surface/heading/Table handles. Assertions inspect actual native cells in all three rows and both columns, and table count remains three after the title change. Empty-shell regression window: `482160`.

`native-evidence/fake-initial.png` and `native-evidence/fake-updated.png` were visually inspected; matching JSON files record window title, identity, visibility and transport. Evidence captures native content; window chrome/identity is covered by the assertions.

Edited Rust/Swift sources were touched before incremental checks. Each deliberately broken implementation failed, then was reverted before the final green app/native run:

1. Key ItemIds by display name instead of source record token. `cargo test --locked --manifest-path apps/srtop/Cargo.toml identity_survives_reorder_rename_pid_change_and_source_changes_do_not_alias` → expected exit 101; ItemId(3) versus ItemId(1). `mutation-identity-red.log`.
2. Reverse the sampled record order. `cargo test --locked --manifest-path apps/srtop/Cargo.toml injected_source_is_called_once_and_three_rows_are_model_data` → expected exit 101; exact ordered model-value mismatch. `mutation-order-red.log`.
3. Project unavailable PID as numeric zero. `cargo build --locked --manifest-path apps/srtop/Cargo.toml` → exit 0, then `swift test --package-path client-macos --filter ProcessExplorerShellTests` → expected exit 1; native cell `"0"` versus `"Unavailable"`. `mutation-native-build.log`, `mutation-native-red.log`.

## Bounded setup recovery and limitations

An initial log redirection failed because the external implementation directory had not yet been created; creating that run directory resolved it before test execution.

The first native entry-point attempt stopped in the pre-existing parallel shutdown fixture: `tests/shutdown.rs:21:31`, `Os { code: 17, kind: AlreadyExists, message: "File exists" }`. PID plus wall-clock nanoseconds can collide between test threads on this host. The fixture now adds a process-local atomic sequence to its private directory name. Product shutdown behavior is unchanged. The original failure is retained in `native.log`; the exact entry point passed after this test-only repair in `native-after-fixture-repair.log` and again after mutations in `native-final.log`.

This is one fixed fake snapshot, not live Linux/macOS collection, periodic refresh, sorting/filtering, action safety, release readiness or htop parity. No remote Linux host or live collector behavior is claimed. Full unrelated repository suites and delivery hooks are the orchestrator's later delivery checks. Ignored build products remain temporarily for the active delivery run as requested; all logs, result JSON and native evidence remain outside the checkout. No push, PR or follow-on ticket was started.
