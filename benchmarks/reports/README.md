# Committed benchmark report

baseline.json is the machine-readable result and baseline.md is its human-readable rendering.
They are host-specific evidence, not cross-machine pass thresholds. Regenerate them only through
scripts/run-benchmarks --profile full --record-baseline so an ordinary run cannot silently replace
reviewed numbers.
