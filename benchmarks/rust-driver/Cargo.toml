[package]
name = "srui-benchmark-driver"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[dependencies]
prost = "0.13"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
portable-pty = "0.8"
srui-protocol = { path = "../../server-rust/protocol" }
srui-semantic-tree = { path = "../../server-rust/semantic-tree" }
srui-journal = { path = "../../server-rust/journal" }
srui-event-dedupe = { path = "../../server-rust/event-dedupe" }
srui-pty = { path = "../../server-rust/pty" }
