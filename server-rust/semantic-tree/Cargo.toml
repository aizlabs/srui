[package]
name = "srui-semantic-tree"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
srui-protocol = { path = "../protocol" }

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"

